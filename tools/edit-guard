#!/usr/bin/env bash
# Point-in-time host check, not an atomic editing lock. Keep other coders paused.
set -uo pipefail
exec python3 -B "$(dirname -- "${BASH_SOURCE[0]}")/estate_processes.py" guard "$@"
